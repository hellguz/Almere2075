# Almere 2075 App Environment Variables
# This is an example file. Copy it to a new file named .env
# and replace the placeholder values with your actual API keys.
# The .env file is ignored by Git and should NOT be committed.

# Backend API Keys
OPENAI_API_KEY="sk-..."
REPLICATE_API_KEY="r8_..."

# The base URL of the application, used for constructing image URLs if needed
APP_URL="https://almere.i-am-hellguz.uk"

# Docker Compose Settings
# The external port mapping for Nginx
NGINX_HOST_PORT=2075

# MODIFIED: Added Database URL for SQLite
# The path should be relative to the WORKDIR inside the container (/app)
DATABASE_URL="sqlite:////app/database/almere_app.db"


