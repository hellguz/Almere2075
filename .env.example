# Almere 2075 App Environment Variables
# This is an example file. Copy it to a new file named .env
# and replace the placeholder values with your actual API keys.

# --- REQUIRED: Backend API Keys ---
OPENAI_API_KEY="sk-..."
REPLICATE_API_KEY="r8_..."

# --- OPTIONAL: Deployment & Docker Settings ---
# The base URL of the application, used for constructing image URLs if needed
APP_URL="http://localhost:2075"
# The external port mapping for Nginx
NGINX_HOST_PORT=2075

# The location of the SQLite database file within the container.
# This path is relative to the /app directory in the backend container.
# The `database` folder is mounted as a persistent volume.
DATABASE_URL="sqlite:///database/almere_app.db"


